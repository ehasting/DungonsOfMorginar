use std::collections::VecDeque;


struct NonBlockingTerminal {
    let processed_input_queue: VecDeque<(i32, char)> = VecDeque::new();
    let terminal: Terminal;
    pub fn process_key_press_event_queue()
    {

    }
    pub fn get_Rendering_table_lookup_key()
    {

    }
}

